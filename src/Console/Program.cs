using System.Threading.Tasks;

namespace ConsoleApp
{
   internal class Program
   {
      private static async Task Main()
      {
         //await UseCaseExamples.BlobStorage();
         //await UseCaseExamples.DataLake();
         //await UseCaseExamples.KeyVault();
         //await UseCaseExamples.ServiceBusTopicSender();
         //await UseCaseExamples.ServiceBusTopicSubscriptionListner();
      }
   }
}